#include <bits/stdc++.h>
using namespace std;
#define all(x) (x).begin(), (x).end()
#define sz(x) (int)x.size()
#define int long long
#define endl '\n'
const int inf = (int)INT_MAX, N = (int)1e6;

void solve() {
    
}
signed main() {
    cin.tie(0)->sync_with_stdio(0);cin.exceptions(ios::badbit | ios::failbit);
    int tc = 1; cin >> tc; while(tc--) solve();
}